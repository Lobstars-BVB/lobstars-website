---
interface Props {
  id?: string;
  cssClasses?: string;
}

const { id, cssClasses } = Astro.props;
---

<section
  id={id}
  class={`mx-auto max-w-screen-xl px-4 py-20 md:px-10 ${cssClasses}`}
>
  <slot />
</section>
